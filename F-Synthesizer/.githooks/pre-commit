#!/usr/bin/env sh

# Personal lightweight mode:
# pre-commit does not auto-update markdown files to avoid timestamp-only noise.
# Run docs updates manually when needed (e.g. weekly maintenance).

exit 0
