# CLAUDE.md

このリポジトリでは `F-Synthesizer/AGENTS.md` を AI エージェント向けの唯一の入口にします。
最初にそのファイルを読んでください。

セッション開始時:

1. `git status --short --branch` を実行する。
2. `F-Synthesizer/README.md` を読む。
3. `F-Synthesizer/AGENTS.md` に従う。
