#!/usr/bin/env sh

pwsh -NoProfile -ExecutionPolicy Bypass -File "F-Synthesizer/scripts/update_synth_docs.ps1"
status=$?
if [ $status -ne 0 ]; then
  echo "pre-commit: update_synth_docs.ps1 failed" >&2
  exit $status
fi

exit 0
