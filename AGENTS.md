# AGENTS.md

## 目的と優先順位

手持ちの MIDI に好みの音色を手軽に割り当て、自分版の曲を作る。
個人開発で、現在の Windows PC と身内程度の利用を想定する。
実装は AI に任せたい。学習のための自作や商用展開は目的にしない。

- メガドライブ、X68000、コナミ系の FM サウンドが好み。
- プリセット選択と微調整を中心に、初心者でも操作しやすくする。
- 再生中の音色変更を速く反映する。ドラムはセット単位で扱う。
- 簡単な音符編集を維持する。機能の多さより操作の快適さを優先する。
- 付属プリセットは読み取り専用の出発点。保存は曲か自分用のコピーへ。
- 無料で MIT / BSD / zlib などの依存を優先する。
- 旧データ互換、汎用化、大規模テスト基盤の維持を目的にしない。

## 音色制作と命名

- 実機や特定作品の忠実な再現より、使って気持ちよい音を幅広く探す。ゲーム各社の拡張音源・特殊チップによる音の工夫を参考にする。
- 癖のある音を中心に、差し替えると曲の印象が大きく変わる発見を楽しめる音色集にする。
- FMや生成音に限定せず、録音素材も含めて面白い音になることを優先する。無料・扱いやすいライセンスという条件は維持する。
- 特に重視する個性は、うなる低音、金属的な響き、鋭い輪郭、ざらつき。元のMIDIのメロディ・和音とリズムが分かるように保ち、質感や鳴り方で印象を変える。
- 名前は短い英語の愛称を基本とし、パート名は含めず分類表示に任せる。短い日本語の説明で音の特徴を伝える。

付属音色は61個（ドラム11セットを含む）。英語名と日本語の特徴説明を付け、FM・波形合成・PSG・ノイズ・既存レイヤーで発音の輪郭、倍音の時間変化、持続中の揺れを作り分ける。音色一覧はREADMEを参照する。
基本波形Sine / Triangle / Square / SawはSupport分類。単一波形・フィルターバイパス・初期drive/LFO深さ0・sustainLevel 1を保ち、個性を足す目的で加工しない。波形・アナログ方式でsustainLevelが1なら、音に作用しない減衰ノブは表示しない。
「どれも単純で似ている。個性や面白さが足りない」という試聴の評価を踏まえ、音量や明るさだけで違いを作らない。刷新の依頼では付属音色へ直接反映し、依頼がない限り試聴ページは作らない。
「当たり障りない音が増え、説明通りのクセが出ていない」という評価を受け、数の追加より既存音の個性と説明の一致を優先する。特徴の成分を外した比較も行い、主音に埋もれる成分や短すぎる変化を見直す。音量を上げただけ、PCMが少し変わっただけで特徴を確認したとしない。静かな音は用途として残し、全音色を一律に歪ませない。
全ドラムセットはGMの47打音（35〜81）を持つ。既存8方式にbell / shaker / scrape / whistle / woodblock / cuicaを加え、打音の役割を保つ。追加方式は既存DrumConfigの項目を使い、scrape / shakerのpitchDecaySecは擦り・粒の間隔。
ピアノIvory、笛Zephyr、合成コーラスChorale、短い弦Bristle、減衰するベースGourdと変わり種の3セットを追加した。Chorale / BristleはPad分類。録音音源の忠実な再現とは扱わない。
ChoraleはFilterMode::Vocalの3共鳴帯を使用する。cutoffHzが第1帯域、他の帯域はその1.6倍と3.6倍。既存のfilterCutoff変調で共鳴位置を動かす。FMの音量変調は補助レイヤーも含む出力に適用する。レイヤーは共通フィルターを通るので、足した成分が最後に消えていないか確認する。
改訂した音色の好みの評価はユーザーの試用を待つ。生成・数値確認を試聴評価の代わりにしない。旧音色・成分を外した比較・検証WAVは `F-Synthesizer/output/sound-design/` 内の `character-rework/`、追加音色と近い既存音の比較は同じ場所の `new-colors/` と `tactile-colors/`（Git管理外）に保持する。

## 作業の始め方

1. `git status --short --branch` と [README.md](README.md) を読む。
2. 依頼に関係するコードを読み、ユーザーの未コミット変更を保つ。
3. 変更に合った短い検証を実行し、未確認事項を報告する。

## コードの見取り図

実装本体は `F-Synthesizer/`。

| 場所 | 担当 |
|---|---|
| `src/gui/` | 画面、音色・音符編集、保存、音声デバイス |
| `src/project/`, `include/project/` | Instrument と曲のモデル |
| `src/config/` | 共通 JSON の読込・保存・検証 |
| `src/midi/` | midifile とアプリの演奏イベントの接続 |
| `src/app/`, `src/core/` | 曲の演奏・書き出しと音生成の接続 |
| `src/SynthEngine/`, `src/synth/` | ボイス、ymfm 接続、その他の音源とエフェクト |
| `third_party/` | バージョンを固定した ymfm / midifile の原文ソース |

- 音色と metadata は `GUIState.tones[ch].draft.instrument` が一緒に保持する。`GUIToneWorkspace` はチャンネルごとの採用済み・試聴中の音、プリセット別の微調整、通常ノブと詳細編集に共通のUndoを保持する。GUI内で音色スロットへの別の対応表を持たず、曲の楽器IDは `GUIProjectFacade` でチャンネルごとのコピーへ解決する。
- プリセットの試聴キャッシュは内容のrevisionごとに保持する。付属音色を更新しても旧キャッシュで新版を隠さない。曲・作業中の音は自動で差し替えず、一覧で選んだ時に新版を使う。
- 試聴キャッシュとUndoは `ToneSnapshot`（変更不能な元音色への参照と調整値）を保持し、採用済み・編集中の `ToneVersion` だけが調整後の音色を持つ。詳細編集は新しい元音色を作り、共有元を変更しない。workspaceは `toneBases` に元音色を一度ずつ保存する。既存の埋め込み形式も読み込める。名前付き曲では試聴履歴を組み立てない。
- `GUIProjectFacade` が編集状態と `ProjectModel` を変換し、チャンネル割当を解決する。`AudibleInstrument` で比較中に鳴る音を解決し、`RenderSound` が音量補正を適用する。
- JSON は `config::ProjectToJSON / ProjectFromJSON / WriteJSONFile` を共通利用する。
- JSONはファイル境界で一度解析し、設定ローダー間では解析済みの値を渡す。音色の出力は `config::SoundToJSON` に集約し、内部で文字列化・再解析しない。
- `GUIStatePersistence` は workspace と名前付き曲を保存する。`.fsynth` は MIDI 本体も含む。
- `GUIPresetIO` が一覧・音色コピーの読込・自分用音色の新規保存と名前変更を担当する。保存先は `config/user_presets/`。画面や音色編集からプリセットJSONを直接操作しない。
- 実行経路は `ProjectModel -> RenderConfig -> SynthEngine`。別の音生成・保存経路を増やさない。
- 再生中の設定は `LiveRenderMailbox` へ immutable なスナップショットを公開し、生成側が64サンプル以下の区切りで受け取る。
- MIDI・設定変更を区切りに、音声生成の固定係数をブロックごとに準備する。波形・変調・エンベロープはサンプルごとに進める。並列化はチャンネル数だけでなく音源・レイヤーの負荷も見て判断し、合成順と即時反映の区切りを維持する。
- `PreviewAudio` は約20msのリングを使う。音声コールバックへ可変GUI参照やファイル処理を渡さない。
- 音声デバイスの生成・再生成・破棄はGUIスレッドにそろえ、GUIのCOMは起動から終了までSTAに保つ。短命の生成スレッドでminiaudioのCOMを初期化すると、再生後のファイルダイアログが停止する。生成側は準備済みデバイスだけを開始する。
- ループは都度生成する。古い PCM を使い回して編集反映を失わないようにする。
- `YmfmVoice` はボイスごとにチップを保持し、miniaudio で出力サンプルレートへ変換する。
- 画面は `src/gui/main/Studio*.cpp` と `StepSequencer.cpp`。各ファイルは内部ヘッダーで依存を宣言する。`GUIMain` が起動ループ、素材の初期化・破棄、`FileActions` の寿命を管理する。詳細音源編集は `channeleditor/SourceEditor.cpp` へ音色と打音の選択を明示的に渡す。
- `GUITransport` が曲の移動・一音試聴・自動再開を扱い、`GUIRenderJob` が再生・試聴・書き出しのジョブ開始と終了処理をまとめる。生成ジョブの完了と音声バッファの再生完了を混同しない。生成中の音を `AudioScope` へ送り、GUI が波形を描く。
- GUIのtick・秒変換は `midi::TempoMap` を共通利用し、端数の丸めは呼び出し側で選ぶ。音を鳴らすサンプル位置の変換は引き続きmidifileを使う。
- 音量補正は `assets/ui/preset-levels.json` の実測値をプリセットのコピーへ設定する。`InstrumentConfig.comparisonGain` として曲・お気に入りにも保存し、再生と WAV に同じ補正を使う。

## ビルド

Visual Studio 2022 の C++ ビルドツールを使う。IDE を開く必要はない。
利用者の入口はルートの `start.cmd`。Release をビルドして GUI を起動する。
`start.cmd --build-only` はビルドのみ。通常の開発用チェックは Debug。

この PC の既存依存は Dear ImGui、GLFW、miniaudio、nlohmann/json。
ymfm（BSD-3-Clause）と midifile（BSD-2-Clause）は同梱済みで、ビルド時のダウンロードは不要。
固定リビジョン・著作権・配布時の原文は [サードパーティ通知](F-Synthesizer/licenses/THIRD_PARTY_NOTICE.md) を参照。

環境を作り直す場合の GUI 依存:

```powershell
git clone https://github.com/microsoft/vcpkg.git C:\vcpkg
& C:\vcpkg\bootstrap-vcpkg.bat
& C:\vcpkg\vcpkg.exe install glfw3:x64-windows imgui[glfw-binding,opengl3-binding,docking-experimental]:x64-windows
& C:\vcpkg\vcpkg.exe integrate install
```

## 検証

リポジトリ直下から実行する。

```powershell
.\F-Synthesizer\scripts\check.ps1
.\F-Synthesizer\scripts\check.ps1 -RunRuntimeSmoke
.\F-Synthesizer\scripts\check_presets.ps1
.\F-Synthesizer\scripts\check_persistence.ps1
```

通常はビルドのみ。音声・MIDI変更では短尺 runtime smoke、付属音色変更では preset check を追加する。
保存・再生反映の変更では、ビルド後に persistence check を使う。
同スクリプトは既存オブジェクトを再利用し、隔離した場所で曲保存・原本保護・MIDI時刻・FM音程・再生中の反映も確認する。
GUI変更では必要に応じて短く表示を確認する。`FSYNTH_CAPTURE` に BMP の出力先を設定すると、非表示の実画面を撮影して終了し、作業状態を上書きしない。`FSYNTH_CAPTURE_VIEW` は `controls` / `compact-controls` / `notes` / `drums` / `playing` を指定できる。大規模な自動化基盤は追加しない。
`check_persistence.ps1 -RunAudioDeviceSmoke` は実機出力で一音試聴後の再開・シーク・再生後のファイルダイアログ・音声デバイスの再生成を確認する。ダイアログは非表示で自動操作する。付属音色を変更したときは同スクリプトの `-CalibratePresetLevels` で比較音量を再測定する。
操作感と音の好みはユーザーの試用を受けて調整する。

## 今後の扱い

合意した音源接続・即時反映・画面整理・曲保存・プリセット刷新は実装済み。
次の UI 方針として **D 案（波形・プリセット一覧・お気に入り）を採用**した。
常設プリセット一覧と M PLUS 1・Lucide を反映した画面案に加え、進行バー・3つのノブ・ミックス操作・追加調整の展開を反映した更新案も、ユーザーが確認し了承した。以下をデザインと主要操作の基準にする。本体への組込みは実装済み。以下を現行 UI の操作基準にする。

- Dear ImGui を継続し、X68000 を思わせる直線的で控えめな見た目にする。表示項目を絞り、必要な調整だけ開く。
- 画面案の配色を基準にする: 背景 `#1A252C`、波形面 `#111C24`、本文 `#E6E9E6`、補助文字 `#9FB2BB`、選択の目印 `#AFD9D0`、未採用の目印 `#E1AD75`。角丸のない面と細い区切りを使い、M PLUS 1 Regular を本文、Medium を見出しに用いる。
- 左に選択チャンネルの波形（曲全体へ切替可）、右に選択中のパートのプリセット一覧、下にお気に入りを置く。3候補の提示と音色選択のたびに開くダイアログは使わない。球体案は採用しない。
- 使用中のチャンネルを画面上部に並べ、直接クリックして切り替える。右の常設プリセット一覧は分類アイコン・音色名・短い特徴を2行で表示する。
- MIDI 読込時は元の楽器情報から各パートを仮分類し、ユーザーが変更できるようにする。この分類を右のプリセット一覧の初期絞り込みに使う。
- 曲全体の進行バーを画面上部に常設し、クリックで再生位置を移動する。同じバーをドラッグして繰り返し範囲を指定し、小節の区切りに合わせる。音の形を表示する波形とは別に扱う。
- 上部に未採用の変更の印、一覧に「採用済み／試聴中／調整済み」、下端に選択チャンネルの状態と一音試聴・取り消し・採用を置く。「採用前」と比較している間は、保持中の音を鳴っているように表示しない。
- 常設の音色調整は「明るさ・ざらつき・余韻」の3項目。基本の項目と位置をそろえ、その音色で使えない項目は非表示にする（無効表示や、残った項目の詰め直しにはしない）。丸いノブを上下ドラッグし、修飾キー併用で微調整する。試作では Shift で微調整、右クリックで数値入力を試せるようにした。
- 普段のノブはプリセットの特徴を保つ穏やかな微調整を目的とする。比較時はプリセット間の聞こえる音量を近づけ、音量差に左右されず音色を選びやすくする。具体的な調整範囲と音量補正の加減は実音で調整する。
- 追加の「鳴り方・揺れ」は波形を少し縮めてその下に展開し、波形と右のプリセット一覧を見ながら調整する。立ち上がりや揺れなどを分かりやすい項目で扱い、専門的な FM 設定はさらに「詳細」から開く。
- 音色調整の近くに「元に戻す／やり直す」の小さな2ボタンを置き、Ctrl+Z / Ctrl+Y でも一操作ずつ戻せるようにする。試した音をまとめて採用前へ戻す「取り消す」と区別する。
- 選択中のパートの「音量・消音・このパートだけ聴く」を常設し、左右の位置は必要なときに開く。音量・左右は曲のバランスとして音色の採用／取り消しと別に扱い、音色を取り消しても調整値を残す。
- 一覧で選んだ音色をそのチャンネルへ仮反映し、一音の試聴も可能にする。同一区間の繰り返しと「採用前／試聴中」の切替で比較する。別チャンネルへ移っても仮試聴した音で鳴らし続け、複数パートの変更を一緒に聴けるようにする。
- 編集作業中はチャンネルごと・プリセットごとに微調整を保持する。Aを調整してBを試し、Aへ戻ったときは調整後のAを復元する。
- 音色の選択と微調整をまとめて「採用」し、「取り消す」で採用前へ戻す。お気に入りはどの曲でも使える自分用の音色集とし、調整値を含むコピーを保存する。呼び戻した後の編集で元のコピーを変えない。
- お気に入りは元の音色名でそのまま追加し、後から名前変更できるようにする。下部の常設一覧を維持し、横スクロール・名前検索・分類で探す。
- 曲の保存・WAV書き出し時に未採用の調整があれば、「まとめて採用して続行」か「戻って調整」を選ぶ。終了時の作業状態には未採用の音色とプリセットごとの微調整も保存し、次回起動時に復元する。名前付きの曲ファイルを終了時に上書きしない。
- 曲の再生中の一音試聴は、曲を一時停止して一音を鳴らし、停止した位置から曲を自動再開する。区間リピートと仮試聴の状態を維持する。
- 一音試聴の高さは曲中でそのパートがよく使う音域から自動選択し、手動変更も可能にする。クリックで一定時間鳴らし、長さは設定で変更できるようにする。ドラムは同じ位置の試聴ボタンからキック・スネア・ハイハットなどを使う短い定型ビートを鳴らし、同様に曲を一時停止・自動再開する。
- 音符編集は同じ画面の中央へ切り替える。再生操作、選択チャンネル、試聴・採用の状態は分かる位置に保つ。
- 書体は M PLUS 1（OFL-1.1）、分類・操作アイコンは Lucide（ISC、一部 Feather 由来は MIT）を選定した。音色選択 UI は分類に沿ったアイコンと分類名を併記する。
- コンセプト説明や開発者向けの文章を操作画面へ置かない。操作説明はヘルプやツールチップへ寄せる。

参考試作: `F-Synthesizer/output/ui-proposals/list-study/open-preview.cmd`（Git 管理外、常設一覧・選定素材を反映した ImGui 画面案）。
画面の見本は同ディレクトリの `list-main.png`、追加調整の展開時は `list-controls.png`、音符編集時は `list-notes.png`。操作仕様を追加する前の了承済み画面は `baseline-list-main.png` に保持する。試作上端の未接続表示は本体の UI に含めない。
素材比較: `F-Synthesizer/output/ui-proposals/asset-study/comparison.png`（Git 管理外、選定用の見本）。選定素材を `F-Synthesizer/assets/ui/` へ同梱した。
描画構成の提案は既存 ImGui / GLFW / OpenGL の継続。波形は実音のデータを ImGui で描き、書体は静的 TTF、アイコンは必要な素材を画像化して同梱する。ImPlot / imgui-knobs は調査済みだが追加は提案していない。本体組込みの依頼に従い、この構成で実装する。
試作は音声・ファイル保存未接続で、波形・曲はサンプル。試作中の変更は終了時に消える。
これらは試作の制限であり、本体仕様ではない。了承済みの更新案を本体へ反映した。立ち上がりと余韻は個別に調整する。お気に入り・未採用の音色・プリセットごとの微調整は保存へ接続済み。音の好みと操作感は、ユーザーの試用を受けて調整する。

録音素材が必要になった場合は TinySoundFont（MIT）を検討する。素材自体のライセンスは別に確認する。

## 文書運用

利用者向けは README、AI 向けはこのファイル。ライセンス通知・原文は別に維持する。
機能一覧、方針書、設計評価書、長期計画、完了ログを別文書に増やさない。
具体的な未完了作業が生じたらここへ短く記載し、完了したら消す。履歴は Git を参照する。
